spring:
  flyway:
    enabled: true
  datasource:
    url: jdbc:mysql://localhost:3396/pick_perfume?&serverTimezone=UTC
    username: dev
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: never
  elasticsearch:
    uris:
      http://${elasticsearch.host:localhost}:${elasticsearch.port:9200}
    connection-timeout: 1s
    socket-timeout: 30s


logging:
  level:
    org.springframework.data.elasticsearch: DEBUG




springdoc:
  swagger-ui:
    path: /docs
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /api-docs
  show-actuator: true
  default-consumes-media-type: application/json
  default-produces-media-type: application/json